pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'mvn clean package -DskipTests'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -f Dockerfile_ZhantemirB -t devopsapp:zhantemir .'
            }
        }

        stage('Deploy') {
            steps {
                sh 'docker compose -f docker-compose_ZhantemirB.yml up -d'
            }
        }
    }
}
